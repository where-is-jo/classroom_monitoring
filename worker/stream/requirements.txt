# stream worker 실행에 필요한 패키지.
# FFmpeg과 MediaMTX는 pip 패키지가 아니라 시스템에 따로 설치한다. README를 따른다.
opencv-python>=4.10
numpy>=1.26
pydantic>=2.7
pydantic-settings>=2.3
# config/settings.yml(환경과 무관한 일반 설정·판정 기준값)을 읽는 데 필요하다.
pyyaml

# 테스트
pytest>=8.0
