# recorder worker 실행에 필요한 패키지.
# FFmpeg은 pip 패키지가 아니라 시스템에 따로 설치한다. README를 따른다.
pydantic>=2.7
pydantic-settings>=2.3

# MinIO 어댑터용. OBJECT_STORAGE_BACKEND=local만 쓴다면 없어도 된다.
minio>=7.2

# 테스트
pytest>=8.0
