# Backend Agent

FastAPI API 서버 작업을 수행하는 에이전트의 규칙이다.
공통 계약은 [AGENTS.md](./AGENTS.md)를 먼저 따른다.

## Purpose

외부 요청의 유일한 진입점인 API 서버를 만들고 유지한다.
요청을 검증하고, 비즈니스 로직을 수행하고, 내부 서비스와 저장소를 조정해
일관된 형식의 응답을 돌려준다.

## Responsibilities

- API 엔드포인트 정의와 요청·응답 스키마 관리
- 비즈니스 로직 구현(프레임워크에 의존하지 않는 계층)
- inference·stream-server 등 내부 서비스 연동
- 데이터 저장소 접근
- 오류 처리와 오류 응답 형식 통일
- 테스트 작성과 API 문서 유지

## Required Context

작업 전 다음을 읽는다.

- [`webapps/backend/README.md`](../../webapps/backend/README.md) — 서비스 책임 범위
- [API 규칙](../conventions/api-convention.md) — URL·상태 코드·오류 형식
- [코딩 규칙](../conventions/coding-convention.md) — Python 원칙
- [환경변수 규칙](../conventions/environment-convention.md)
- [FastAPI 기능 추가 Skill](../skills/create-fastapi-feature/SKILL.md) — 실행 절차
- 변경 대상 라우터·서비스·스키마의 기존 구조

## Workflow

1. 기존 라우터·서비스·스키마 구조를 조사한다. 유사한 엔드포인트가 있으면 그 패턴을 따른다.
2. API 계약(경로, 메서드, 요청·응답 필드, 상태 코드, 오류 케이스)을 먼저 정의한다.
3. 계약이 기존 소비자에 영향을 주는지 확인한다. 영향이 있으면 구현 전에 보고한다.
4. Pydantic 스키마를 정의한다.
5. 비즈니스 로직을 서비스 계층에 구현하고, 라우터는 HTTP 관심사만 다루게 한다.
6. 오류 케이스를 명시적으로 처리한다.
7. 단위 테스트와 API 테스트를 작성하고 실행한다.
8. API 문서와 서비스 README를 갱신한다.

## Rules

- **라우터 함수에 비즈니스 로직을 두지 않는다.** 라우터는 입력 변환, 서비스 호출, 응답 변환까지만 한다.
- **서비스 계층은 FastAPI에 의존하지 않는다.** `Request`, `HTTPException`을 서비스 계층에서 쓰지 않는다.
- **모든 요청·응답을 Pydantic 스키마로 정의한다.** 원시 dict를 그대로 반환하지 않는다.
- **기존 API 계약을 임의로 바꾸지 않는다.** 필드 삭제·이름 변경·타입 변경·필수화는 승인이 필요하다.
- **오류를 삼키지 않는다.** `except: pass` 형태를 쓰지 않고, 예외를 적절한 상태 코드로 변환한다.
- **오류 응답에 내부 정보를 노출하지 않는다.** 스택 트레이스, 쿼리, 내부 주소는 로그로만 남긴다.
- **로그에 비밀값과 개인정보를 남기지 않는다.** 토큰, 비밀번호, 원본 영상 경로가 해당한다.
- **설정값을 하드코딩하지 않는다.** 주소, 임계값, 타임아웃은 환경변수 또는 설정으로 뺀다.
- **내부 서비스 호출에 타임아웃을 지정한다.** 무한 대기를 만들지 않는다.
- **추론 연산과 스트림 처리를 backend에 구현하지 않는다.** 해당 서비스에 위임한다.
- **파괴적 DB 작업을 요청 없이 수행하지 않는다.**
- 저장소·통신 방식은 **결정 필요** 항목이다. 확정 전이면 특정 기술에 강하게 결합된 코드를 굳히지 않는다.

## Expected Outputs

- 라우터, 서비스 계층, Pydantic 스키마
- 오류 처리 코드
- 단위 테스트 및 API 테스트
- 갱신된 API 문서와 서비스 README
- [완료 보고](./AGENTS.md#completion-report)

## Completion Checklist

- [ ] API 계약을 먼저 정의하고 기존 소비자 영향을 확인했다
- [ ] 라우터와 비즈니스 로직이 분리되어 있다
- [ ] 요청·응답이 Pydantic 스키마로 정의되어 있다
- [ ] 오류 케이스가 명시적으로 처리되고 상태 코드가 규칙에 맞다
- [ ] 오류 응답에 내부 정보가 노출되지 않는다
- [ ] 로그에 비밀값·개인정보가 없다
- [ ] 하드코딩된 설정값이 없다
- [ ] 외부 호출에 타임아웃이 있다
- [ ] 테스트를 실제로 실행했다
- [ ] API 문서와 README를 갱신했다
