# deeplearning(얼굴 분석 내부 서비스)의 환경변수 목록 기준 문서다.
# 실제 값은 커밋하지 않는다. 컨테이너로 띄울 때는 .docker/compose.main.<환경>.yml의
# environment:가 이 값들을 준다 — 비밀값이 없어 별도 env 파일을 두지 않는다.
#
# 규칙: docs/conventions/environment-convention.md

# --- 모델 가중치 경로 ---
# 셋 다 파일이 실제로 있어야 한다. app.py가 기동(lifespan) 시점에 확인하고
# 없으면 RuntimeError로 죽는다. 요청을 받다가 실패하지 않게 하려는 것이다.
#
# 가중치는 저장소에 없다(.models/는 .gitignore 대상, 합쳐서 448MB).
# 컨테이너에서는 .docker/models/face가 /models/face로 읽기 전용 마운트된다.

# SCRFD 얼굴 검출 모델.
FACE_DETECTION_MODEL_PATH=/models/face/scrfd/scrfd_10g_bnkps.onnx

# MediaPipe Face Landmarker. 자세(yaw·pitch·roll) 추정에 쓴다.
FACE_LANDMARKER_MODEL_PATH=/models/face/mediapipe/face_landmarker.task

# ArcFace 계열 얼굴 인식 모델. 비우면 app.py가 소스 옆 .models/buffalo_l를 보는데,
# 컨테이너에는 그 경로가 없으므로 여기서 명시한다.
FACE_RECOGNITION_MODEL_PATH=/models/face/buffalo_l/w600k_r50.onnx
