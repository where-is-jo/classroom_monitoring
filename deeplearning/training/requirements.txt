# 사람 탐지 모델 학습 노트북에 필요한 패키지.
# 공용 GPU 서버의 torch가 이미 CUDA에 맞게 설치돼 있다면 그 환경을 그대로 쓰고
# 이 파일로 torch를 다시 설치하지 않는다 — GPU 빌드가 CPU 전용으로 덮어써질 수 있다.
ultralytics>=8.3
# Ultralytics ByteTrack의 선형 할당 의존성. 런타임 자동 설치에 의존하지 않는다.
lap>=0.5.12
# 교차 카메라 OSNet-AIN 준비와 Hungarian 매칭
torchreid==0.2.5
gdown>=5.2
tensorboard>=2.20
onnx>=1.17
scipy>=1.14
opencv-python>=4.10
jupyterlab>=4.0
python-dotenv>=1.0
matplotlib>=3.8
pyyaml>=6.0
