// 개발자 PC(local) 스택용 로그 수집 설정. 서버용 config.dev.alloy와 project label만 다르다.
// 같은 docker daemon의 모든 컨테이너 로그를 Docker API로 읽어 Loki로 보낸다.
// compose 스택이 셋으로 나뉘어 있어도 daemon 하나라 한 번에 다 잡힌다.

discovery.docker "containers" {
  host = "unix:///var/run/docker.sock"
}

discovery.relabel "containers" {
  targets = discovery.docker.containers.targets

  rule {
    source_labels = ["__meta_docker_container_name"]
    regex         = "/(.*)"
    target_label  = "container"
  }

  // 어느 compose 스택의 컨테이너인지 구분한다.
  rule {
    source_labels = ["__meta_docker_container_label_com_docker_compose_project"]
    target_label  = "stack"
  }
}

loki.source.docker "containers" {
  host       = "unix:///var/run/docker.sock"
  targets    = discovery.relabel.containers.output
  labels     = {"project" = "classroom-monitoring-local"}
  forward_to = [loki.write.local.receiver]
}

loki.write "local" {
  endpoint {
    url = "http://loki:3100/loki/api/v1/push"
  }
}
